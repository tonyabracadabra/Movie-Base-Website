{>"layouts/master" /}

{<body}
	{#movies}
	{! float in columns !}
		<div class="large-4 columns end">
			<img class="th" src="{.cover}">
			<h3>{.title}<h3>
			<a class="button" href="/movies/details/{._id}">Movies Info</a>
		</div>

		{! if no movies comes here !}

		{:else}
			<p>No movies are found!</p>
	{/movies}
	
{/body}
